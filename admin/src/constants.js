// Base URL of the backend API.
// Points to the local dev server for now — swap this for the deployed
// URL when the backend goes to production.
export const API_BASE_URL = "http://localhost:3000";
