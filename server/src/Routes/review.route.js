import express from "express";

const router = express.Router();

router.post("/createreview",);
router.get("/getreviews",);
router.patch("/updatereview/:reviewId",);
router.delete("deletereview/:reviewId",);

export default router